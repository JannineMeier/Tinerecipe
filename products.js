// products.js
(function(){
  const {AISLE_ORDER,store}=window.AppData;
  const { $,h,flash }=window.UI;

  function initProductsUI(){ /* ... */ }
  function renderProducts(){ /* ... */ }

  window.Products={initProductsUI,renderProducts};
})();
